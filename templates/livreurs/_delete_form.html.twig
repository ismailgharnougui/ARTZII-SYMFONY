<form method="post" action="{{ path('app_livreurs_delete', {'id': livreur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ livreur.id) }}">
    <button class="btn">Delete</button>
</form>

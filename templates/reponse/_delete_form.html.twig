<form method="post" action="{{ path('app_reponse_delete', {'idRep': reponse.idRep}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reponse.idRep) }}">
    <button class="btn">Delete</button>
</form>
